#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'the program's description'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int index = 0;

	while (index < 10)
	{
		printf("%d", index);
		index++;
	}
	putchar('\n');
	return (0);
}
